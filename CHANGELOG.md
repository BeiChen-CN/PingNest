# 更新日志

## [1.0.0] - 2026-08-28

首个正式版本：界面全面升级为 Material Design 3「分层工作台」风格，架构重构与可维护性整理。

### 变更

- 全新 Material Design 3「分层工作台」界面：青色主色、224px 展开式导航抽屉、置顶页标题栏，窄窗口下抽屉自动折叠为图标轨道。
- 通知弹窗与全部四种样式（清晰卡片/速览条/重点卡片/文字提示）按 MD3 视觉语言重绘；外观页新增实时弹窗预览。
- 新版奶油绿圆润 P 应用图标，窗口、托盘与安装包同步更新。
- 关于页版本号改为随 `package.json` 自动显示。

### 修复

- 修复通知弹窗背景出现白色画布、无法透出桌面的问题。
- 修复 WCDB 路径兜底函数中无效嵌套循环的可读性残留（行为不变）。

### 开发者

- 配置类型、消息文案映射与通知尺寸常量收敛到 `shared/`，主进程与渲染层单一来源。
- 主进程巨型模块拆分：WCDB 门面 + FFI/路径/归一/SQL 专项模块，密钥服务拆出 Win32 绑定与进程发现，IPC 分域注册并改为声明式校验。
- 新增统一日志模块（`PINGNEST_LOG_LEVEL` / `PINGNEST_LOG_FILE`），全部静默 catch 均已注明忽略理由。
- 新增 GitHub Actions CI（Windows：typecheck + 单元测试）；测试从 23 项扩充到 40 项，覆盖撤回识别、去重缓存与 SQL 只读校验等核心纯逻辑。
- 图标改为脚本生成（`npm run icon`），源图与生成逻辑入库。

## [0.1.0] - 2026-08-25

PingNest 首个公开版本。

### 新增

- 支持私聊、群聊和公众号桌面通知。
- 提供标准、紧凑、层叠、极简四种通知样式。
- 支持通知位置、透明度、持续时间、提示音和消息合并设置。
- 支持轻盈的进入、替换和退出动画。
- 支持通知点击后激活微信主窗口，或打开 PingNest 通知历史。
- 支持通知历史、会话分组、搜索、日期筛选、类型筛选和本地记录管理。
- 支持按会话、关键词和匹配模式创建静音规则。
- 支持概览、历史、静音、外观、设置和关于页面。
- 支持开机启动、启动至托盘、关闭窗口后驻留托盘和自动重连。
- 支持多显示器通知布局。
- 使用独立 Worker 隔离微信密钥 Hook 与 WCDB 查询。
- 原生监控管道不可用时，自动使用本地轮询后备方案。
- 使用新的 PingNest 消息巢应用图标。

### 修复

- 修复 WCDB `snake_case` 字段和 `computed_is_send` 字段兼容问题。
- 修复私聊历史被错误归类为“其他”的问题。
- 修复群聊显示名称和群成员名称解析问题。
- 修复撤回消息和媒体消息内容映射问题。
- 修复通知点击、通知关闭和自动消失时动画不完整的问题。
- 增强微信主窗口识别，优先按微信进程 PID 激活窗口。
- 增加 DLL 授权环境错误提示，包括 `AUTH_FAILED:auth_env_missing`、`-1006` 和 `-2301`。
- 增加不兼容 DLL 的明确提示，避免误用 WeFlow 或 CipherTalk 授权资源。

### 兼容性与限制

- 当前面向 Windows 10/11 x64 和桌面版微信。
- `resources/` 下的原生 DLL 必须使用与 PingNest 配套的版本，不能跨项目直接替换。
- “激活微信主窗口”不会模拟点击微信内部控件，也不保证直接定位到指定聊天。
- 微信版本、安装方式、系统权限和安全软件可能影响连接结果。

### 发布验证

- `npm run typecheck` 通过。
- `npm test` 通过，23 项测试全部通过。
- `npm run build` 成功生成 Windows 安装包。

### 致谢

感谢 [WeFlow](https://github.com/hicccc77/WeFlow) 提供思路与具体代码参考。PingNest 在此基础上结合自身需求进行了适配、重构和界面实现。
