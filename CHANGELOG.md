# 更新日志

## [1.1.0] - 2026-08-30

审计修复 + 架构升级（SQLite / 增量广播 / 监控伴生库）+ 通知样式 2026 九套重设计 + 工作台 UI 推倒重来（奶油工作台）+ 四套动效系统。

### 变更（动效系统）

- **四套可切换动效方案**（外观页选择，即时生效）：绸缎（默认，丝滑缓动）、水滴（弹性回弹）、墨锋（干脆利落）、漂浮（轻盈浮动）。以 `<html data-motion>` 驱动全局 CSS 变量（缓动/时长/位移/模糊/交错），统一作用于侧栏滑行胶囊、页面进场、卡片交错浮现、控件过渡与悬停节奏。
- 主页数据卡数字滚动计数动画（尊重 `prefers-reduced-motion`，减弱动效时直接显示终值）。

### 新增（原生监控伴生库）

- 集成 `pingnest_monitor.dll` 伴生监控库：`wcdb_api.dll` 未内置监控管道接口时自动加载（先过 SHA256 完整性清单再 `koffi.load`），以 `ReadDirectoryChangesW` 监听微信 `db_storage` 目录，变更经命名管道实时推送 `db_change` 事件；复用既有管道消费端与自适应轮询退避，空闲空转进一步降低。伴生库缺失 / 清单校验失败 / 启动异常均静默降级本地轮询，不阻断连接；启动日志如实标注当前启用的通道。

### 变更（界面推倒重来 · 奶油工作台）

- **渲染层全量重写**：放弃 MD3 全盘化，改为「奶油工作台」设计语言——淡奶油绿主色（#7fa86b 系）+ 墨绿信号块 + 奶油纸底，控件靠细线、留白与少量品牌色组织层级；界面骨架改为侧栏（真图标 + 墨绿填充选中态 + 底部迷你状态卡）+ 顶部命令栏。
- **主页为纯数据 Bento 甲板**：实时消息流（墨绿 hero 块）、未读、连接详情（含健康度条）、静音规则芯片、最近会话、今日概览、本地历史容量、数据安全，全部真实数据；设置与操作归位各功能页。
- **新增 Ctrl+K 命令面板**：页面跳转与常用操作（立即检查/重新连接/开关桌面弹窗）的统一入口。
- 小控件全部重画：按钮（filled/outlined/tonal/danger）、开关、芯片、分段选择、搜索框、列表行、设置行、弹窗、Toast、下拉选择；历史页改为列表+信件式详情双栏。
- 深色模式同一奶油绿语言整体换肤；通知弹窗默认强调色与主窗口背景同步为奶油绿系。

### 变更（通知卡片大小）

- 新增"卡片大小"设置（大 1.15× / 中 标准 / 小 0.85×）：以 CSS zoom 整体缩放九套通知卡片，设计比例与动效原样保留；窗口尺寸按缩放系数同步换算，外观页实时预览联动。

### 变更（通知样式 2026 重设计）

- **九套全新通知样式替换旧五套**：潮汐（液态玻璃流光）、终端（系统日志单行 + 光标闪烁）、信笺（纸质信笺 + 时间邮戳 + 撤回印章）、霓虹弧光（旋转弧光 + 文字辉光）、音轨（信号频谱指示消息活性）、蜂巢（六边蜂室，多会话堆叠时横向拼成蜂窝）、卷轴（热敏纸带接收流水）、呼吸圆环（真实倒计时圆环 + 临期警示脉冲）、灵动胶囊（重制版：壳体由中间向两边展开、内容延后浮现、消失时内容先隐、壳体向中间收拢）。视觉与动效基准：`design/notification-styles-2026.html` 提案页，应用内实现与提案一致。
- 旧样式 ID 自动迁移：`island → capsule`、`standard/layered → tidal`、`compact → terminal`、`minimal → halo`，未知值回退 `capsule`。
- 外观页选择器更新为九选一（每组带特色预览色板），实时预览直接渲染真实通知组件。
- 堆叠队列适配：呼吸圆环的倒计时环即停留进度；蜂巢样式堆叠时窗口横向扩展为蜂窝排布。
- 各样式入场动画按提案逐一套用（潮汐模糊浮起、信笺抽信、卷轴打印、胶囊中心展开等），退场默认按位置方向，胶囊/卷轴/蜂巢/圆环/潮汐有专属退场。

### 变更（架构决策）

- **通知历史迁移到 SQLite（L2）**：改用 Electron 内置 Node 的 `node:sqlite`（零新增原生依赖），WAL 模式随写随持久化，取代 300ms 防抖全量 JSON 重写；旧 `notify-center.json` 首启自动迁移入库并保留原文件，损坏走 `.corrupt-` 备份。行级加密语义与旧存储一致（`safeStorage` 可用 → `enc:`，不可用按行回退明文）。改进：跨环境无法解密的旧加密历史不再永久阻塞新写入（降级提示 + 恢复原环境后自动重试迁移）；"降级到旧版再升级"不再丢数据（迁移标记仅在完成后写入）。
- **通知堆叠队列（L3）**：新增"同屏堆叠卡片"设置（1–6，默认 3）。多会话消息不再"最后者胜"式互相替换，而是同屏堆叠；超出上限时最旧卡片播放退场动画，全部退场后窗口自动隐藏；同会话合并仍生效且合并卡片移至最靠角落位置。设为 1 可回到单卡替换行为。通知窗口尺寸上限随卡片数扩展且不超过工作区高度。
- **深色模式跟随系统（UX-04）**：基于 MD3 token 层新增暗色变量集（`prefers-color-scheme`），全部界面与通知样式整体换肤；原生窗口背景色随 `nativeTheme` 同步，避免暗色下加载白闪。

### 修复

- **透明通知弹窗的"画布"问题彻底根治**：`nativeTheme` 主题切换不再重涂透明通知窗口背景色；移除全部样式的投影与 backdrop 滤镜（透明窗口上投影会渲染成桌面上的半透明画布，AMD 合成器下尤甚）；霓虹弧光盘收进卡片并 `overflow: hidden`；内容先行显示流程（先投递内容、渲染完成后再呈现窗口）消除"先闪空框再跳变"。
- **通知卡片样式与主窗口全局样式串扰**：卡片内部类名全面 `ns-*` 命名空间化，实时预览不再被页面样式污染。
- **侧栏滑行胶囊颜色溢出**：胶囊改用 `getBoundingClientRect` 相对测量 + `top: 0` 确定原点（旧实现 `offsetTop` 以边框盒为原点、绝对定位以 padding 盒为原点，两者相差侧栏内边距，胶囊恒比选中行低 16px、压住下一行上半截）；补滚动/字体加载/尺寸变化重测，测量前隐藏。
- 主窗口顶栏补齐最小化 / 最大化 / 关闭三键（lucide 图标、贴齐窗口右缘、关闭键悬停红），移除顶栏重复的应用图标；动效选择等分段控件过长时自动换行。
- dbWorker 进程异常退出后自动复位连接/监控状态：重建 worker 后原生监控可恢复，不再永久退化为轮询。
- WCDB 同步连续失败时状态上报 UI：概览页显示"消息同步异常"及原因，状态徽标同步反映，不再静默显示"监听中"。
- 通知历史持久化降级可见：加密历史无法解密、损坏备份失败时暂停写入并在设置页提示；首次启动不再误报"历史损坏"。
- `wx_key.dll` 加载失败与密钥进程超时/异常退出的错误文案补充下一步操作指引。

### 安全

- 渲染层注入 Content-Security-Policy（构建产物严格白名单；dev 兼容 HMR）。
- 主窗口与通知窗口显式 `sandbox: true`，统一拦截 `will-navigate` 与 `window.open`。
- `config:set` 补齐 `dbPath/decryptKey/myWxid/myWxName` 类型校验与静音规则扩展子字段校验，校验规则拆分为纯模块并新增测试。
- 原生 DLL 完整性校验：构建期生成 `resources/dll-manifest.json`（SHA256），`wx_key.dll` / `wcdb_api.dll` / `WCDB.dll` / `SDL2.dll` 加载前逐一校验，不匹配即拒载并给出重装指引；UNC 路径的临时缓存副本同样校验、失效即重建。
- `npm audit` 归零：jimp 升级至 1.6.1（消除 file-type GHSA-5v7r-6r5c-r473 漏洞链），图标脚本同步迁移到 jimp v1 API，重新生成的图标与原版逐字节一致。
- 隐私披露补全：关于页与概览页明确"联系人头像经微信 CDN 拉取"这一运行期唯一外联点；README 许可证章节补充 CC BY-NC-SA 的非商业边界、ShareAlike 传染范围与第三方组件许可指引。

### 性能与可靠性

- `app:getStatus` 的微信进程检测结果缓存 10 秒，消除渲染层高频轮询带来的 tasklist 进程开销。
- 日志文件 sink 增加 5MB 轮转（`.old.log`），长期运行不再无上限增长。
- 通知中心改为增量广播（`notify-center:update` 只携带新增/更新/删除条目）：万条级历史时单条消息广播从全量数组的 MB 级降到 KB 级；渲染层按 ID 合并，形状异常回退全量拉取。
- 轮询间隔可配置（设置页 1–15 秒），原生监控管道活跃时自动降频到 max(5×, 10 秒)。
- SQL 分表路径的群消息发送者改为按批 `IN (...)` 反查（每批 500），消除逐行 N+1 查询。
- 历史会话详情分页渲染（默认最近 200 条，可"加载更早"），超长会话不再一次性渲染数千 DOM 节点。

### 开发者

- 消息推送核心抽取为 `messagePushService`（纯依赖注入、零 electron 导入），支持合成消息源直连测试；新增基线/推送/去重/撤回/过滤/降级 7 个用例。
- `dbWorkerClient` 的 fork 工厂可注入，新增 5 个协议契约测试（请求-响应配对、超时、退出拒答与自动重启、monitor 分发、dispose 清理）。
- `ruleEngine` 去 electron 化（单例移至 main.ts 组装），新增 10 个纯逻辑用例（matchMode 组合、大小写、畸形条目跳过、效果字段透传）。
- IPC 通道名收敛到 `shared/ipcChannels.ts` 单一来源，preload 与 ipcMain 两侧统一引用，删除/改名通道时 typecheck 直接报错。
- 联系人头像、头像 URL 与群昵称三处缓存增加条目软上限与过期清理，长期运行不再只增不减。
- 构建新增 `afterAllArtifactBuild` 钩子，产出 `release/SHA256SUMS.txt` 供发布渠道公示产物哈希。
- 新增 `npm run licenses` 生成运行时依赖第三方许可清单，构建时自动执行并随安装包分发（`resources/licenses/`）。
- 移除无调用方的 `notification:show` IPC 通道与 kernel32 Win32 绑定；`hasFullConfig` 复用 `hookReady` 计算。
- README 更正消息监听机制描述（2 秒轮询为常驻通道而非后备）。
- 通知布局测试更新为九套样式 + 卡片尺寸缩放 + 旧样式 ID 迁移；新增配置校验、伴生监控管道名/返回码纯逻辑测试。测试从 40 项扩充到 **88 项**。
- 设计提案页（UI 重设计 / 通知样式 2026 / 动效方案）归档至 `design/`，不再作为 `public/` 静态资源进入构建产物。

## [1.0.0] - 2026-08-28

首个正式版本：界面全面升级为 Material Design 3「分层工作台」风格，架构重构与可维护性整理。

### 变更

- 全新 Material Design 3「分层工作台」界面：青色主色、224px 展开式导航抽屉、置顶页标题栏，窄窗口下抽屉自动折叠为图标轨道。
- 通知弹窗与全部四种样式（清晰卡片/速览条/重点卡片/文字提示）按 MD3 视觉语言重绘；外观页新增实时弹窗预览。
- 新版奶油绿圆润 P 应用图标，窗口、托盘与安装包同步更新。
- 关于页版本号改为随 `package.json` 自动显示。

### 修复

- 修复通知弹窗背景出现白色画布、无法透出桌面的问题。
- 修复 WCDB 路径兜底函数中无效嵌套循环的可读性残留（行为不变）。

### 开发者

- 配置类型、消息文案映射与通知尺寸常量收敛到 `shared/`，主进程与渲染层单一来源。
- 主进程巨型模块拆分：WCDB 门面 + FFI/路径/归一/SQL 专项模块，密钥服务拆出 Win32 绑定与进程发现，IPC 分域注册并改为声明式校验。
- 新增统一日志模块（`PINGNEST_LOG_LEVEL` / `PINGNEST_LOG_FILE`），全部静默 catch 均已注明忽略理由。
- 新增 GitHub Actions CI（Windows：typecheck + 单元测试）；测试从 23 项扩充到 40 项，覆盖撤回识别、去重缓存与 SQL 只读校验等核心纯逻辑。
- 图标改为脚本生成（`npm run icon`），源图与生成逻辑入库。

## [0.1.0] - 2026-08-25

PingNest 首个公开版本。

### 新增

- 支持私聊、群聊和公众号桌面通知。
- 提供标准、紧凑、层叠、极简四种通知样式。
- 支持通知位置、透明度、持续时间、提示音和消息合并设置。
- 支持轻盈的进入、替换和退出动画。
- 支持通知点击后激活微信主窗口，或打开 PingNest 通知历史。
- 支持通知历史、会话分组、搜索、日期筛选、类型筛选和本地记录管理。
- 支持按会话、关键词和匹配模式创建静音规则。
- 支持概览、历史、静音、外观、设置和关于页面。
- 支持开机启动、启动至托盘、关闭窗口后驻留托盘和自动重连。
- 支持多显示器通知布局。
- 使用独立 Worker 隔离微信密钥 Hook 与 WCDB 查询。
- 原生监控管道不可用时，自动使用本地轮询后备方案。
- 使用新的 PingNest 消息巢应用图标。

### 修复

- 修复 WCDB `snake_case` 字段和 `computed_is_send` 字段兼容问题。
- 修复私聊历史被错误归类为“其他”的问题。
- 修复群聊显示名称和群成员名称解析问题。
- 修复撤回消息和媒体消息内容映射问题。
- 修复通知点击、通知关闭和自动消失时动画不完整的问题。
- 增强微信主窗口识别，优先按微信进程 PID 激活窗口。
- 增加 DLL 授权环境错误提示，包括 `AUTH_FAILED:auth_env_missing`、`-1006` 和 `-2301`。
- 增加不兼容 DLL 的明确提示，避免误用 WeFlow 或 CipherTalk 授权资源。

### 兼容性与限制

- 当前面向 Windows 10/11 x64 和桌面版微信。
- `resources/` 下的原生 DLL 必须使用与 PingNest 配套的版本，不能跨项目直接替换。
- “激活微信主窗口”不会模拟点击微信内部控件，也不保证直接定位到指定聊天。
- 微信版本、安装方式、系统权限和安全软件可能影响连接结果。

### 发布验证

- `npm run typecheck` 通过。
- `npm test` 通过，23 项测试全部通过。
- `npm run build` 成功生成 Windows 安装包。

### 致谢

感谢 [WeFlow](https://github.com/hicccc77/WeFlow) 提供思路与具体代码参考。PingNest 在此基础上结合自身需求进行了适配、重构和界面实现。
